No direct runtime introspection of the live ABBY/CLAW swarm or its internal orchestration code is available from this agent session. The sandbox environment is isolated (stateless disposable VM with no access to the running orchestrator, memory, vault contents, or agent runtime beyond tool outputs). GitHub repo BOS-AURA is public and contains the orchestrator dashboard; the sandbox_exec tool cannot read/modify the live deployed system.
