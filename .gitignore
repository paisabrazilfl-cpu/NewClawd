# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
# EXCEPTION: the two SERVED dist dirs are committed and shipped to Render (the
# deploy model serves the prebuilt dist). They MUST be tracked, otherwise a new
# entry-bundle hash (which changes on every app-code change) is silently dropped by
# `git add` and production 404s the JS/CSS → blank page. Un-ignore them explicitly.
!/artifacts/api-server/dist/
!/artifacts/openclaw/dist/
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# env / secrets — never commit real keys; use .env.example as the template
.env
.env.*
!.env.example

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Replit
.cache/
.local/
.replit
attached_assets/

# self-test artifacts (screenshots, reports)
.self-test/

# Scratch/experimental scripts — keep out of version control (may hold raw keys)
experiments/

# One-off demo artifact handed to the operator (proof build, not production code)
big-blue-racer.html
